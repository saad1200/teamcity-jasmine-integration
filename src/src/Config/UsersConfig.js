
function UsersConfig (usersPresenter, usersController){

	var ini = function()
	{
			// var userId = 10;

	// // var usersController = new UsersController(usersPresenter, userService);



	// // var usersConfig = new UsersConfig(usersPresenter, usersController);
	// var user = usersService.getUser(userId);

	// alert(user.name);
	};

	ini();
}